/**
 * Game Machine Constants
 *
 * Shared constants used across the XState machine implementation.
 */

/** Minimum number of players required to start a game */
export const MIN_PLAYERS = 5;

/** Maximum number of players allowed in a game */
export const MAX_PLAYERS = 11;

/** Duration of quiz rounds in milliseconds (30 seconds) */
export const QUIZ_DURATION_MS = 30 * 1000;

/** Default number of questions per player in quizzes */
export const QUESTIONS_PER_PLAYER = 3;
